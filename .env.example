# PostgreSQL Configuration for pgAdmin
# Replace 'YOUR_PGADMIN_PASSWORD' with your postgres master password from pgAdmin
DATABASE_URL=postgresql://postgres:YOUR_PGADMIN_PASSWORD@localhost:5432/revguard

# Fallback SQLite Database (used if PostgreSQL credentials are invalid)
# DATABASE_URL=sqlite:///./revguard.db

PROJECT_NAME=REVGUARD
SECRET_KEY=sox_audit_secret_key_revguard_2026
DEBUG=True
